const makeTrackEntity = require('./createTrack');
const updateTrackEntity = require('./updateTrack');


const trackEntity = Object.freeze({
    makeTrackEntity,
    updateTrackEntity
});

module.exports= trackEntity;
module.exports = {
    makeTrackEntity,
    updateTrackEntity
};